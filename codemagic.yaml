# codemagic.yaml
workflows:
  build-iris-metal:
    name: Build Iris Metal Mod
    instance_type: mac_mini_m2          # 如果机器是 Intel，请改为 mac_mini

    scripts:
      - name: Install JDK 25 (Temurin)
        script: |
          # 回到根目录（保险）
          cd $CM_BUILD_DIR
          # 下载 JDK 25 (Apple Silicon)
          curl -L -o jdk.tar.gz \
            https://github.com/adoptium/temurin25-binaries/releases/download/jdk-25%2B36/OpenJDK25U-jdk_aarch64_mac_hotspot_25_36.tar.gz
          sudo mkdir -p /Library/Java/JavaVirtualMachines
          sudo tar -xzf jdk.tar.gz -C /Library/Java/JavaVirtualMachines
          # 找到实际解压后的目录名（可能是 jdk-25+36）
          JDK_DIR=$(ls /Library/Java/JavaVirtualMachines | grep jdk-25 | head -1)
          # 设置 JAVA_HOME 并写入环境文件
          export JAVA_HOME="/Library/Java/JavaVirtualMachines/$JDK_DIR/Contents/Home"
          echo "export JAVA_HOME=$JAVA_HOME" >> $CM_ENV
          # 验证版本
          $JAVA_HOME/bin/java -version

      - name: Compile Swift Native Library
        script: |
          cd $CM_BUILD_DIR
          cd src/main/native
          # 如果是 Intel 机器，将 -target 改为 x86_64-apple-macos14.0
          swiftc -O -target arm64-apple-macos14.0 \
            -framework Foundation -framework Metal -framework MetalKit \
            -framework QuartzCore \
            -emit-library IrisMetalNative.swift \
            -o ../resources/natives/macos/libiris_metal.dylib
          cd $CM_BUILD_DIR

      - name: Build Mod Jar with Gradle
        script: |
          cd $CM_BUILD_DIR
          # 加载我们设置的 JAVA_HOME
          source $CM_ENV
          echo "Using JAVA_HOME: $JAVA_HOME"
          $JAVA_HOME/bin/java -version

          # 如果 gradlew 不存在，自动生成
          if [ ! -f gradlew ]; then
            echo "gradlew not found, generating..."
            brew install gradle
            gradle wrapper
          fi

          # 确认文件存在
          ls -la gradlew

          # 赋予执行权限
          chmod +x gradlew

          # ★ 关键：通过 -Dorg.gradle.java.home 强制指定 JDK
          ./gradlew build --no-daemon -Dorg.gradle.java.home="$JAVA_HOME"

    artifacts:
      - build/libs/*.jar
